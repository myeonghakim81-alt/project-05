# Copy this file to .env and fill in your own Supabase project's values to
# switch learner-state storage from local-only to Supabase.
# Leave both unset (or delete .env) to keep using local storage — the app
# works fully offline either way (see src/lib/storage.ts).
#
# Get these for free at https://supabase.com -> New project -> Project Settings -> API
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=

# Optional — see docs/gemini.md. Leave unset to use the built-in scripted
# roleplay dialogues instead of live AI conversation.
EXPO_PUBLIC_GEMINI_API_KEY=
